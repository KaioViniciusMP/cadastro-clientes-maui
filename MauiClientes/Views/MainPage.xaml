<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiClientes.Views.MainPage"
             Title="Cadastro de Clientes">

    <StackLayout Padding="20">
        <Label Text="Clientes Cadastrados" FontSize="16" FontAttributes="Bold" Margin="0,0,0,20" />

        <ListView ItemsSource="{Binding Customers}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Padding="10" Margin="0,5"  CornerRadius="5">
                            <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                                <StackLayout VerticalOptions="Center" HorizontalOptions="StartAndExpand">
                                    <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold"/>
                                    <Label Text="{Binding Lastname}" FontSize="14"/>
                                    <Label Text="{Binding Age}" FontSize="14"/>
                                    <Label Text="{Binding Address}" FontSize="14"/>
                                </StackLayout>

                                <StackLayout Orientation="Horizontal" HorizontalOptions="End" Spacing="5">
                                    <Button Text="Editar" BackgroundColor="DodgerBlue" WidthRequest="150" HeightRequest="60" TextColor="White" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.EditCommand}" CommandParameter="{Binding .}" />
                                    <Button Text="Apagar" BackgroundColor="#F44336" WidthRequest="150" HeightRequest="60" TextColor="White" Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}" CommandParameter="{Binding .}" />
                                </StackLayout>
                            </StackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="Adicionar Cliente" Clicked="OnAddCustomerClicked" BackgroundColor="DodgerBlue" TextColor="White"/>
    </StackLayout>
</ContentPage>